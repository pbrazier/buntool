* [X] When loading the uploaded files into the table, automatically sort by file name.
* [X] Add a new feature that will allow other file types like images and office documents to be added and then converted.
* [X] Add the file name to the header of each page. Ensure in the step 3 section of the UI and backend logic that the page numbering options are updated to reflect this.
* [X] Understand how we could add AI features into this. That will automatically categorise and section the uploaded files.
* [X] Also consider an option using AI to read the files and make a structured and robust name convention.
* [X] When opening files to upload the file selection window loads twice ignoring the first file selection.
