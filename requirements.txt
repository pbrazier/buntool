Flask==3.1.0
Werkzeug==3.1.3
waitress==3.0.2
pypdf==5.1.0
pikepdf==9.5.1
pdfplumber==0.11.5
reportlab==4.3.0
python-docx==1.1.2
Pillow==11.1.0

# Optional: AI features (install the one matching your provider)
# pip install openai        # For OpenAI (GPT-4o, etc.)
# pip install anthropic     # For Anthropic (Claude, etc.)
# pip install google-genai  # For Google (Gemini, etc.)